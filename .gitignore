node_modules/
test-results/
playwright-report/
dist/
*.tsbuildinfo
.DS_Store
.env
server/.env

# DB / runtime data
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Logs
*.log

# Go
# The panel serves the two linux builds from /api/agent/binary/<asset>, so they
# are tracked; every other local build stays out of the repository.
agent/bin/*
!agent/bin/yaws-agent-linux-amd64
!agent/bin/yaws-agent-linux-arm64
!agent/bin/SHA256SUMS
*.exe

# Archives
*.zip
